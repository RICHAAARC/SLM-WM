# Fixtures

此目录保存可提交的小型测试 fixture。fixture 只能用于单元测试、约束测试或轻量功能测试, 不应包含真实大规模实验结果、生成图像集合、模型权重或 Google Drive 下载副本。

需要保留的运行产物应写入 `outputs/` 下的语义子目录并默认不提交。重型验证应放入显式 opt-in 的集成测试或服务器运行流程, 不得进入默认 `pytest -q` 成本路径。
